apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    certificatesResolvers:
      letsencrypt:
        acme:
          email: kjyy08@gmail.com
          storage: /data/acme.json
          httpChallenge:
            entryPoint: web
    
    logs:
      general:
        level: INFO
        filePath: /logs/traefik.log
      access:
        enabled: true
        filePath: /logs/access.log
    
    metrics:
      prometheus:
        enabled: true
    
    otlp:
      http:
        endpoint: http://otel-collector.observability:4318
        insecure: true
    
    ports:
      web:
        port: 80
        exposedPort: 80
        redirectTo:
          port: websecure
          permanent: true
      websecure:
        port: 443
        exposedPort: 443
        tls:
          enabled: true
    
    persistence:
      enabled: true
      path: /data
      size: 1Gi
      storageClass: bulk-hdd
    
    volumes:
      - name: traefik-logs
        hostPath:
          path: /data/k3s-storage/traefik
          type: DirectoryOrCreate
        mountPath: /logs